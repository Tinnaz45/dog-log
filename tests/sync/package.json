{
  "name": "dog-log-sync-tests",
  "private": true,
  "description": "WORK-136 PR-B client sync tests. Disposable local PostgreSQL + a local Supabase emulator; never contacts Supabase DEV or PROD.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1",
    "pg": "8.16.3"
  }
}
